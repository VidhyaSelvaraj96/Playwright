//VAR
var currentCountry ="India"
var currentCountry ="Deutschland"
currentCountry=2026
console.log(currentCountry);

//var value can be redeclared and reassigned
//latest assigned (reassigned) value will be the output

//LET
/* let job ="Automation"
let job ="Manual"
job = "Automation-Playwright"
console.log(job);
*/

//Cannot execute above. ERROR - Identifier "job" is already declared
//let can be redeclared but cannot be reassigned

let job ="Automation"
//let job ="Manual"
job = "Automation-Playwright"
console.log(job);

//CONST
/*const pi=3.12
const pi=3.33
pi=1.2
console.log(pi);
*/

//Cannot execute above. ERROR - Identifier "pi" has already decalared
//let can be redeclared but cannot be reassigned

const pi=3.12;
console.log(pi);

//also, const variable and value should be on the same line
