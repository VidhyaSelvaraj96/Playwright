const { log } = require("console");

let numArray=[1,2,4,6,8]

//length of the array
console.log(numArray.length); //op-5

//indexOf() - starts from 0
console.log(numArray.indexOf(4)); //op-2

//adding number - after the end (total index in the array is 4)
console.log(numArray[5]='Herz');
console.log(numArray);
